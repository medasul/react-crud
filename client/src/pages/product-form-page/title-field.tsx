import React from 'react';
import { TextField } from '@mui/material';

const TitleField = () => (
  <TextField
    label="Title"
    fullWidth
    variant="filled"
    size="small"
  />
);

export default TitleField;
